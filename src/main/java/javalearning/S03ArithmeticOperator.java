package javalearning;

public class S03ArithmeticOperator {

	public static void main(String[] args) {

		// Mathematics priorities essential.
		// Casting is convert from datatype to another.
		// Double > Float > Integer
		int i = 9, j = 2;
		System.out.println((float) i / j);

		// Increments and decrements (pre ++i "add then compile and do the code", post
		// i++ "compile and do the code then add").

		// i +=5 --> i = i + 5.

		/*
		 * Logical operator: 1 - && means "and", so if the two conditions is true the
		 * value will be true . 2 - || means "or", so if one of the conditions is true
		 * the value will be true.
		 */
	}

}
